[supervisord]
nodaemon=true
user=root

[program:mariadb]
command=/usr/sbin/mysqld --user=mysql
autorestart=true
priority=5

[program:php-fpm]
command=/usr/local/sbin/php-fpm
autorestart=true
priority=10

[program:nginx]
command=/usr/sbin/nginx -g "daemon off;"
autorestart=true
priority=20
